steps:
  - label: ":wave: :apple:"
    agents:
      queue: "macos"
      tart: "true"
    plugins:
        - ./.buildkite/plugins/tart:
          cpus: 4
    command: |
      uptime
      sw_vers
      system_profiler SPHardwareDataType -json
